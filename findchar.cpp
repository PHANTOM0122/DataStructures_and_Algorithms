#include <iostream>

using namespace std;

/*
bool find_char(const char* s, char ch) {
	while (*s != '\0') {
		if (*s == ch)
			return true;
		s++;
	}
	return false;
}

int main() {
	const char* phrase = "this is a phrase";
	for (char ch = 'a'; ch <= 'z'; ch++) {
		if (find_char(phrase, ch))
			cout << ch << " is in " << phrase << endl;
		else
			cout << ch << " is not in " << phrase << endl;
	}
}*/

int main() {
	int a[] = { 2,3,4 };
	cout << a;
}